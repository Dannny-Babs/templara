---
"@templara/editor": minor
---

Viewport-aware toolbar/inspector dropdowns, friendly layer labels, large-schema data panel browse (hide `$` fields + ancestor search), and `preparePreviewData` for real-record hydrate.
